{
  "name": "mysterious-server",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "engines": {
    "node": ">=18.0.0 <21.0.0",
    "npm": ">=10.0.0 <11.0.0"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "build": "echo 'No build step required for now'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "nodemon": "^2.0.15"
  },
  "overrides": {
    "semver": ">=7.6.0"
  }
}
